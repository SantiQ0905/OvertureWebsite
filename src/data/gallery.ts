export interface GalleryImage {
  src: string;
  alt: string;
}

// TODO: populate with real team photos from public/assets once supplied —
// renders an on-brand empty state until then.
export const galleryImages: GalleryImage[] = [];
