export interface TeamVideo {
  youtubeId: string;
  title: string;
}

// TODO: populate with real YouTube video IDs once supplied — renders an
// on-brand empty state until then.
export const teamVideos: TeamVideo[] = [];
